import { InboundMessage } from "../message/inbound";
import { SocketMessageListener } from "./socket";
export interface ListenerObject {
    reqId: string;
    callback: SocketMessageListener;
}
export declare class EoswsListeners {
    registeredListeners: ListenerObject[];
    private debug;
    constructor(options?: {
        id?: string;
    });
    handleMessage(message: InboundMessage<any>): void;
    addListener(listener: ListenerObject): void;
    removeListener(reqId: string): void;
}
